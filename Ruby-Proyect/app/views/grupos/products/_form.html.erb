<%= form_with(model: grupos_product) do |form| %>
  <% if grupos_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grupos_product.errors.count, "error") %> prohibited this grupos_product from being saved:</h2>

      <ul>
        <% grupos_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
